import { axiosInstance } from '@/shared/api';
import { {{camelCase name}}Schema, {{plural name}}Schema } from '../model/{{kebabCase name}}.schema';

export async function get{{pascalCase (plural name)}}() {
  const { data } = await axiosInstance.get('/{{kebabCase (plural name)}}');
  return {{plural name}}Schema.parse(data);
}
