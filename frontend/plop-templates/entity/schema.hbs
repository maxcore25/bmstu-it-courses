import { z } from 'zod';

export const {{camelCase name}}Schema = z.object({
  id: z.uuid(),
  name: z.string(),
  price: z.number().nonnegative(),
  createdAt: z.string(),
  updatedAt: z.string(),
});

export type {{pascalCase name}} = z.infer<typeof {{camelCase name}}Schema>;

export const {{plural name}}Schema = z.array({{camelCase name}}Schema);
